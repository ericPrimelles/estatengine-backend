extend type Query {
    getHousesForSaleListings(
        city: String
        state: String
        zipcode: String
        page: Int!
        limit: Int!
    ): HouseForSaleListingResponse!
}

type HouseForSaleListingResponse {
    data: [HouseForSaleListing]
    message: String!
}
type HouseForSaleListing {
    title: String!
    description: String!
    url: AWSURL!
}